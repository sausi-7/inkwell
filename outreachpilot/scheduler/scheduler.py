"""APScheduler-based scan scheduling (Phase 1 — placeholder for now)."""

import logging

logger = logging.getLogger(__name__)


def init_scheduler():
    """Initialize the scheduler. Placeholder for Phase 1."""
    logger.info("Scheduler not yet configured (Phase 1 feature)")
